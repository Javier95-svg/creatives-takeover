// This file is automatically generated. Do not edit it directly.
import { createClient } from '@supabase/supabase-js';
import type { Database } from './types';
import { getEnv } from '@/lib/env';

// Prefer environment variables so keys are not committed to source.
// In development create a local `.env` with VITE_SUPABASE_URL and VITE_SUPABASE_KEY (do NOT commit it).
const isProd = (import.meta.env as any).PROD === true || (import.meta.env as any).MODE === 'production';

// Read envs using the helper. In production we want to fail fast; in dev we warn.
const SUPABASE_URL = getEnv('VITE_SUPABASE_URL', isProd);
const SUPABASE_PUBLISHABLE_KEY = (() => {
  const primary = getEnv('VITE_SUPABASE_KEY', false);
  const alt = getEnv('VITE_SUPABASE_PUBLISHABLE_KEY', false);
  const val = primary || alt;
  if (!val && isProd) {
    // If running in production and there is no key, throw so deployment fails loudly.
    throw new Error('Missing required Supabase publishable key (VITE_SUPABASE_KEY or VITE_SUPABASE_PUBLISHABLE_KEY)');
  }
  if (!val) {
    // eslint-disable-next-line no-console
    console.warn('Supabase publishable key not found; functionality will be limited in dev.');
  }
  return val;
})();

// Guard localStorage for SSR/build contexts
const storage = (typeof window !== 'undefined' && typeof window.localStorage !== 'undefined') ? window.localStorage : undefined;

export const supabase = createClient<Database>(SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY, {
  auth: {
    storage,
    persistSession: true,
    autoRefreshToken: true,
  }
});